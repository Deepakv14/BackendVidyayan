# BackendVidyayan Deployment
- Create a file named as '.gitignore' and add '/node_modules/'
- Create another file named as 'vercel.json' 
- Make some engine related changes in package.json
- Push on github
